Place the Mule Standalone Runtime here as `mule-standalone.zip` before running `docker build`.

Download it (free, no Enterprise license needed to run — see the main README) from:
https://www.mulesoft.com/lp/dl/anypoint-mule-studio

This file is intentionally not committed to the repository (proprietary binary, ignored via `.gitignore`).
